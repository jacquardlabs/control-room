"""control-room: the operator seat for concurrent Claude Code work.

This package currently holds the T1 stream-discovery story only: finding
live interactive sessions, Workflow runs, and background tasks from disk,
with liveness bookkeeping (grace/age-out) but no attention semantics yet.
"""
